const kobita = 'Jemas'
const priya = "Asif"
const multiline = `1. Root name server (.). It stores the IP addresses of Top Level Domain (TLD) name servers. There are 13 logical root name servers globally.

2. TLD name server. It stores the IP addresses of authoritative name servers. There are several types of TLD names. For example, generic TLD (.com, .org), country code TLD (.us), test TLD (.test).

3. Authoritative name server. It provides actual answers to the DNS query. You can register authoritative name servers with domain name registrar such as GoDaddy, Namecheap, etc. `

const friends =['dipok','sobuj','jahid']
const age = 28;
const backtickSign = `Shahadat Hossain ${age}.Work Smart.${friends.length}`
console.log(backtickSign);

const f = 'md'
const l = 'sh'
const full = `This Person ${f} ${l}`